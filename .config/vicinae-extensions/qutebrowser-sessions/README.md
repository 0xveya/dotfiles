# Qutebrowser Sessions

Vicinae extension for managing qutebrowser sessions from a native list UI.

Quickstart with `bun`:

```bash
bun install
bun run dev
```

Production build:

```bash
bun run build
```
